import SearchPage from '../../components/user/SearchPage';

export const metadata = {
  title: 'Search Hostels & Bachelor Rooms',
  description: 'Search and filter verified hostels, bachelor rooms, PGs and shared rooms by city, price, sharing type and amenities.',
};

export default function Page() {
  return <SearchPage />;
}
